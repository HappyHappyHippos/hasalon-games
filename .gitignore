node_modules/
dist/
*.log

# The usage log's default home (ANALYTICS_FILE). Real people's names, and in
# production it lives on a mounted volume rather than in the repo either way.
data/
.env
.env.local
.DS_Store
.vite/
coverage/

# Throwaway test bots and probes. CLAUDE.md's "one browser tab plus a
# scratch ws bot" recipe tells you to write these at the repo root and delete
# them afterwards; this is so the ones you forget stay out of `git status`.
*.tmp.mjs
*.tmp.ts
*.tmp.js
